/*
    Incomplete return at line 7
    Unclosed function call at line 14
*/

int gcd(int u, int v) {
    if (v ==0) return
    else return gcd(v, u - u/v*v);
    /* note that u- u/v*v = u mod v */
}

void main(void) {
    int x; int y;
    x = input(); y = input();
    output(gcd(x, y);
}
